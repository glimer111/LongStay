# Пример конфига nginx для Long Stay с раздачей загрузок по /uploads/
# Скопируйте в /etc/nginx/sites-available/ и сделайте симлинк в sites-enabled.
# Замените YOUR_DOMAIN на свой. Путь alias — каталог, куда приложение сохраняет файлы (по умолчанию public/uploads).

upstream long_stay_next {
    server 127.0.0.1:3000;
    keepalive 64;
}

server {
    listen 80;
    server_name YOUR_DOMAIN;   # например longstay.example.com или 199.68.199.143

    # Загрузки — отдаём статику напрямую (путь должен совпадать с UPLOAD_DIR или public/uploads)
    location /uploads/ {
        alias /var/www/longstay/public/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Всё остальное — Next.js
    location / {
        proxy_pass http://long_stay_next;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
